apiVersion: v1
kind: Service
metadata:
  name: rider-service
  namespace: rideshare
spec:
  type: ClusterIP
  selector:
    app: rider-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
